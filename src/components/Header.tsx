import styled from "styled-components";

const Root = styled.div`
  height: 80px;
  background-color: #fff;
  display: flex;
`;

export const Header = () => {
  return (
    <Root>
      Header
    </Root>
  );
};
